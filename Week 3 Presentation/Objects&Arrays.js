/**
 * Objects are collections of key-value pairs where keys are strings (or Symbols)
 * and values can be any type.
 * 
 * Arrays are ordered collections of values, 
 * which can be of any type, indexed by non-negative integers.
 * 
 * 
 */

//ARRAYS
var myArray = [1, 2, 3, 4 ,5]; // array of numbers 
 //            ^        ^  
            //index 0   index 3

// Accessing Arrays by index
console.log(myArray[3]); // Accesses index 3 of myArray and logs to console
console.log(myArray[Array.length]); // can access array and log the length of entire array            